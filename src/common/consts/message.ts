const USER_NOT_FOUND = 'User not found';

const USER_ALREADY_EXISTS = 'User already exists';

const INVALID_TOKEN = 'Invalid token';

const EMAIL_ALREADY_EXISTS = 'Email already exists';

const USERNAME_ALREADY_EXISTS = 'Username already exists';

const INVALID_DEFAULT_POSITION = 'Default position not found';

const POSITION_NOT_FOUND = 'Position not found';

const INVALID_SECRET_KEY = 'Invalid secret key';

const WORKSHEETS_IS_EMPTY = 'Worksheets is empty';

const FILE_NOT_FOUND = 'File not found';

const ROLE_NOT_FOUND = 'Role not found';

const SYSTEM_ROLE_CANNOT_BE_DELETED = 'System role cannot be deleted';

export {
  USER_NOT_FOUND,
  USER_ALREADY_EXISTS,
  INVALID_TOKEN,
  EMAIL_ALREADY_EXISTS,
  USERNAME_ALREADY_EXISTS,
  INVALID_DEFAULT_POSITION,
  POSITION_NOT_FOUND,
  INVALID_SECRET_KEY,
  WORKSHEETS_IS_EMPTY,
  FILE_NOT_FOUND,
  ROLE_NOT_FOUND,
  SYSTEM_ROLE_CANNOT_BE_DELETED,
};
